    public class CxXmlConfigClass1385680659 { public CxXmlConfigClass() {















if (MANIFEST) {    MANIFEST.XMLNS_ANDROID = "http://schemas.android.com/apk/res/android";
     MANIFEST.PACKAGE = "com.google.android.test.activity";     MANIFEST;
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.GET_TASKS";     MANIFEST.USES_PERMISSION;}
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.REORDER_TASKS";     MANIFEST.USES_PERMISSION;}
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.REMOVE_TASKS";     MANIFEST.USES_PERMISSION;}
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.READ_FRAME_BUFFER";     MANIFEST.USES_PERMISSION;}
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.INTERACT_ACROSS_USERS";     MANIFEST.USES_PERMISSION;}
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.INTERACT_ACROSS_USERS_FULL";     MANIFEST.USES_PERMISSION;}
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.MANAGE_USERS";     MANIFEST.USES_PERMISSION;}
if (APPLICATION) {    MANIFEST.APPLICATION.ANDROID_LABEL = "ActivityTest";     MANIFEST.APPLICATION;
if (ACTIVITY) {    MANIFEST.APPLICATION.ACTIVITY.ANDROID_NAME = "ActivityTestMain";     MANIFEST.APPLICATION.ACTIVITY;
if (INTENT-FILTER) {
if (ACTION) {    MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.ACTION.ANDROID_NAME = "android.intent.action.MAIN";     MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.ACTION;}
if (CATEGORY) {    MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.CATEGORY.ANDROID_NAME = "android.intent.category.LAUNCHER";     MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.CATEGORY;}}
}

if (SERVICE) {    MANIFEST.APPLICATION.SERVICE.ANDROID_NAME = "SingleUserService";
     MANIFEST.APPLICATION.SERVICE.ANDROID_SINGLEUSER = "true";     MANIFEST.APPLICATION.SERVICE.ANDROID_EXPORTED = "true";     MANIFEST.APPLICATION.SERVICE;}

if (SERVICE) {    MANIFEST.APPLICATION.SERVICE.ANDROID_NAME = "ServiceUserTarget";     MANIFEST.APPLICATION.SERVICE;}

if (RECEIVER) {    MANIFEST.APPLICATION.RECEIVER.ANDROID_NAME = "UserTarget";     MANIFEST.APPLICATION.RECEIVER;}

if (RECEIVER) {    MANIFEST.APPLICATION.RECEIVER.ANDROID_NAME = "SingleUserReceiver";
     MANIFEST.APPLICATION.RECEIVER.ANDROID_SINGLEUSER = "true";     MANIFEST.APPLICATION.RECEIVER.ANDROID_EXPORTED = "true";     MANIFEST.APPLICATION.RECEIVER;}

if (PROVIDER) {    MANIFEST.APPLICATION.PROVIDER.ANDROID_NAME = "SingleUserProvider";
     MANIFEST.APPLICATION.PROVIDER.ANDROID_AUTHORITIES = "com.google.android.test.activity.single_user";
     MANIFEST.APPLICATION.PROVIDER.ANDROID_SINGLEUSER = "true";     MANIFEST.APPLICATION.PROVIDER.ANDROID_EXPORTED = "true";     MANIFEST.APPLICATION.PROVIDER;}}
}
    } }

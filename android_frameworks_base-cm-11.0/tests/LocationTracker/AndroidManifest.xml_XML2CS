    public class CxXmlConfigClass2045550130 { public CxXmlConfigClass() {if (MANIFEST) {    MANIFEST.XMLNS_ANDROID = "http://schemas.android.com/apk/res/android";
     MANIFEST.PACKAGE = "com.android.locationtracker";     MANIFEST;


if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.ACCESS_FINE_LOCATION";     MANIFEST.USES_PERMISSION;}
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.ACCESS_COARSE_LOCATION";     MANIFEST.USES_PERMISSION;

}
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.ACCESS_WIFI_STATE";     MANIFEST.USES_PERMISSION;

}
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.WAKE_LOCK";     MANIFEST.USES_PERMISSION;

}
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.WRITE_EXTERNAL_STORAGE";     MANIFEST.USES_PERMISSION;}

if (APPLICATION) {    MANIFEST.APPLICATION.ANDROID_LABEL = "@string/app_label";     MANIFEST.APPLICATION;
if (ACTIVITY) {    MANIFEST.APPLICATION.ACTIVITY.ANDROID_NAME = "TrackerActivity";     MANIFEST.APPLICATION.ACTIVITY.ANDROID_LABEL = "Location Tracker";     MANIFEST.APPLICATION.ACTIVITY;
if (INTENT-FILTER) {
if (ACTION) {    MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.ACTION.ANDROID_NAME = "android.intent.action.MAIN";     MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.ACTION;}
if (CATEGORY) {    MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.CATEGORY.ANDROID_NAME = "android.intent.category.LAUNCHER";     MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.CATEGORY;}}
}

if (SERVICE) {    MANIFEST.APPLICATION.SERVICE.ANDROID_NAME = ".TrackerService";     MANIFEST.APPLICATION.SERVICE;}
if (ACTIVITY) {    MANIFEST.APPLICATION.ACTIVITY.ANDROID_LABEL = "@string/settings_menu";     MANIFEST.APPLICATION.ACTIVITY.ANDROID_NAME = "SettingsActivity";     MANIFEST.APPLICATION.ACTIVITY;}
if (PROVIDER) {    MANIFEST.APPLICATION.PROVIDER.ANDROID_NAME = ".data.TrackerProvider";
     MANIFEST.APPLICATION.PROVIDER.ANDROID_AUTHORITIES = "com.android.locationtracker";     MANIFEST.APPLICATION.PROVIDER;}}
}

    } }

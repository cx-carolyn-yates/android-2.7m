    public class CxXmlConfigClass1514033912 { public CxXmlConfigClass() {if (MANIFEST) {    MANIFEST.XMLNS_ANDROID = "http://schemas.android.com/apk/res/android";
     MANIFEST.PACKAGE = "com.android.frameworkperf";     MANIFEST;
if (USES-PERMISSION) {    MANIFEST.USES_PERMISSION.ANDROID_NAME = "android.permission.WAKE_LOCK";     MANIFEST.USES_PERMISSION;}
if (USES-SDK) {    MANIFEST.USES_SDK.ANDROID_MINSDKVERSION = "5";     MANIFEST.USES_SDK;}

if (APPLICATION) {    MANIFEST.APPLICATION.ANDROID_HARDWAREACCELERATED = "false";     MANIFEST.APPLICATION;
if (USES-LIBRARY) {    MANIFEST.APPLICATION.USES_LIBRARY.ANDROID_NAME = "android.test.runner";     MANIFEST.APPLICATION.USES_LIBRARY;}
if (ACTIVITY) {    MANIFEST.APPLICATION.ACTIVITY.ANDROID_NAME = "FrameworkPerfActivity";     MANIFEST.APPLICATION.ACTIVITY.ANDROID_LABEL = "Framework Perf";     MANIFEST.APPLICATION.ACTIVITY;
if (INTENT-FILTER) {
if (ACTION) {    MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.ACTION.ANDROID_NAME = "android.intent.action.MAIN";     MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.ACTION;}
if (CATEGORY) {    MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.CATEGORY.ANDROID_NAME = "android.intent.category.DEFAULT";     MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.CATEGORY;}
if (CATEGORY) {    MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.CATEGORY.ANDROID_NAME = "android.intent.category.LAUNCHER";     MANIFEST.APPLICATION.ACTIVITY.INTENT_FILTER.CATEGORY;}}
}

if (SERVICE) {    MANIFEST.APPLICATION.SERVICE.ANDROID_NAME = "SchedulerService";     MANIFEST.APPLICATION.SERVICE;}

if (SERVICE) {    MANIFEST.APPLICATION.SERVICE.ANDROID_NAME = "TestService";     MANIFEST.APPLICATION.SERVICE.ANDROID_PROCESS = ":test";     MANIFEST.APPLICATION.SERVICE;}

if (SERVICE) {    MANIFEST.APPLICATION.SERVICE.ANDROID_NAME = "LocalTestService";     MANIFEST.APPLICATION.SERVICE;}

if (RECEIVER) {    MANIFEST.APPLICATION.RECEIVER.ANDROID_NAME = "Receiver";     MANIFEST.APPLICATION.RECEIVER.ANDROID_EXPORTED = "true";     MANIFEST.APPLICATION.RECEIVER;}
}


if (INSTRUMENTATION) {    MANIFEST.INSTRUMENTATION.ANDROID_NAME = "android.test.InstrumentationTestRunner";
     MANIFEST.INSTRUMENTATION.ANDROID_TARGETPACKAGE = "com.android.frameworkperf";
     MANIFEST.INSTRUMENTATION.ANDROID_LABEL = "Framework Perf Runner";     MANIFEST.INSTRUMENTATION;
}}
    } }
